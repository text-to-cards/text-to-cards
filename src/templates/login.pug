extends base.pug

block content
    div(class='auth-container')
        div Text to Cards needs your authorization for creating cards on your boards.
        div
            button(class='mod-primary authorize-button') 👍 Yeah, baby!
